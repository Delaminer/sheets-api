{
  "name": "sheets-api",
  "version": "0.1.0",
  "description": "Server for sheets-api",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm install && npm run build"
  },
  "author": "Alex de la Iglesia",
  "engines": {
    "node": "16.13.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "fs": "^0.0.1-security",
    "path": "^0.12.7"
  }
}
